import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
// change then dependences
import App from '../src/components/savanna/app';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root'),
);
